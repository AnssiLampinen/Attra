-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.customer_events (
  id bigint NOT NULL DEFAULT nextval('customer_events_id_seq'::regclass),
  tenant_id text NOT NULL,
  customer_id bigint NOT NULL,
  title text NOT NULL,
  event_date date NOT NULL,
  event_time time without time zone,
  duration_minutes integer,
  notes text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT customer_events_pkey PRIMARY KEY (id),
  CONSTRAINT customer_events_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES public.tenants(id),
  CONSTRAINT customer_events_customer_id_fkey FOREIGN KEY (customer_id) REFERENCES public.customers(id)
);
CREATE TABLE public.customer_tags (
  customer_id bigint NOT NULL,
  tag_id bigint NOT NULL,
  CONSTRAINT customer_tags_pkey PRIMARY KEY (customer_id, tag_id),
  CONSTRAINT customer_tags_customer_id_fkey FOREIGN KEY (customer_id) REFERENCES public.customers(id),
  CONSTRAINT customer_tags_tag_id_fkey FOREIGN KEY (tag_id) REFERENCES public.tags(id)
);
CREATE TABLE public.customers (
  id bigint NOT NULL DEFAULT nextval('customers_id_seq'::regclass),
  tenant_id text NOT NULL,
  name text,
  phone text,
  email text,
  status text DEFAULT 'unknown'::text,
  summary text,
  profile_notes text,
  customer_profile text,
  last_processed_message_id text,
  last_updated_at timestamp with time zone,
  whatsapp_id text,
  instagram_id text,
  messenger_id text,
  telegram_id text,
  signal_id text,
  twitter_id text,
  linkedin_id text,
  slack_id text,
  discord_id text,
  google_messages_id text,
  google_chat_id text,
  google_voice_id text,
  notes text,
  pinned boolean NOT NULL DEFAULT false,
  needs_refresh boolean NOT NULL DEFAULT false,
  display_name text,
  CONSTRAINT customers_pkey PRIMARY KEY (id),
  CONSTRAINT customers_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES public.tenants(id)
);
CREATE TABLE public.deals (
  id bigint NOT NULL DEFAULT nextval('deals_id_seq'::regclass),
  tenant_id text NOT NULL,
  customer_id bigint,
  title text NOT NULL,
  description text,
  amount numeric,
  status text NOT NULL DEFAULT 'lead'::text,
  expected_close_date date,
  closed_at timestamp with time zone,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT deals_pkey PRIMARY KEY (id),
  CONSTRAINT deals_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES public.tenants(id),
  CONSTRAINT deals_customer_id_fkey FOREIGN KEY (customer_id) REFERENCES public.customers(id)
);
CREATE TABLE public.raw_messages (
  id bigint NOT NULL DEFAULT nextval('raw_messages_id_seq'::regclass),
  tenant_id text NOT NULL,
  customer_id bigint NOT NULL,
  messages jsonb NOT NULL,
  latest_message_id text NOT NULL,
  fetched_at timestamp with time zone NOT NULL DEFAULT now(),
  processed boolean NOT NULL DEFAULT false,
  processed_at timestamp with time zone,
  processing boolean NOT NULL DEFAULT false,
  CONSTRAINT raw_messages_pkey PRIMARY KEY (id),
  CONSTRAINT raw_messages_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES public.tenants(id),
  CONSTRAINT raw_messages_customer_id_fkey FOREIGN KEY (customer_id) REFERENCES public.customers(id)
);
CREATE TABLE public.tags (
  id bigint NOT NULL DEFAULT nextval('tags_id_seq'::regclass),
  tenant_id text NOT NULL,
  name text NOT NULL,
  color text,
  CONSTRAINT tags_pkey PRIMARY KEY (id),
  CONSTRAINT tags_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES public.tenants(id)
);
CREATE TABLE public.tenants (
  id text NOT NULL,
  name text NOT NULL,
  api_key text NOT NULL UNIQUE,
  supabase_user_id uuid,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  username text,
  hide_personal_contacts boolean NOT NULL DEFAULT false,
  display_name text,
  voice_note_append_to_notes boolean NOT NULL DEFAULT true,
  CONSTRAINT tenants_pkey PRIMARY KEY (id),
  CONSTRAINT tenants_supabase_user_id_fkey FOREIGN KEY (supabase_user_id) REFERENCES auth.users(id)
);